# {{ project_name }}

**Student:** {{ author }}  
**Course:** {{ course }}  
**Lab:** {{ lab_number }} — {{ lab_title }}

{{ description }}

## Set up the project

```bash
uv sync --dev
uv run pre-commit install
```

## Run the tests

```bash
uv run pytest
```

## Format the code

```bash
uv run black .
uv run pre-commit run --all-files
```

The pre-commit hook only formats Python files with Black. It does not lint the
code or enforce additional style rules.

## Assignment requirements

- [ ] Add the assignment requirements here.
- [ ] Replace the placeholder function in `src/{{ package_name }}/__init__.py`.
- [ ] Replace the placeholder test and add tests for every requirement.
- [ ] Complete the pseudocode and integrity statement in `docs/`.

## Project structure

```text
.
├── docs/
│   ├── integrity-statement.md
│   └── pseudocode.md
├── src/{{ package_name }}/
│   └── __init__.py
├── tests/
│   └── test_lab.py
├── .pre-commit-config.yaml
├── pyproject.toml
└── README.md
```
